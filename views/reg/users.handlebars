    <nav class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bbb" aria-expanded="false">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                
                        <a href='/see_menu' class="navbar-brand">See Menu</a>

        
             </div>

            <div class="collapse navbar-collapse" id="bbb">
                <ul class="nav navbar-nav navbar-right">
                    {{#if user}}
                    <li>
                        <a href="/logout">logout</a>
                    </li>
                    <li>
                        <a href="/changepassword">Password Change</a>
                    </li>
                    {{/if}}

                </ul>
            </div>
        </div>
    </nav>

    <div class="row">
    <div class="col-md-4 col-md-offset-4">
        {{!-- <h1>Sign In</h1> --}}
        {{#if message}}
            <div class="alert alert-info">
            <p>{{message}}</p>
            </div>
        {{/if}}

        {{#if user}}
                    <div class="alert alert-success">

            <h2>{{user.username}} have logged in</h2>
            </div>
            {{else}}
        <form action="/login" method="post">
            <div class="form-group">
                <label>Username</label>
                <input type="text" name="username" placeholder="philbarrassi" class="form-control">
            </div>
            <div class="form-group">
                <label for="password">Password</label>
                <input type="password" name="password" placeholder="Your Password" class="form-control">
            </div>
            <div class="form-group">
                <label>Verify {{firstNum}} + {{secondNum}} =</label>
                <input type="text" name="vSum" placeholder="Please calculate Sum" class="form-control">
            </div>
            <button type="submit" class="btn btn-primary" value ="login">Sign In</button>
        </form>
        <p>Don't have an account? <a href="/register">Sign up instead!</a></p>
        {{/if}}
    </div>
    </div> 
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>